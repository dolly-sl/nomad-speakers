{%- liquid
  assign kit_count = 0
  for block in section.blocks
    if block.type == 'kit_card'
      assign kit_count = kit_count | plus: 1
    endif
  endfor
-%}
{% if section.settings.video != blank or section.settings.image != blank or section.settings.title != blank or section.settings.pretitle != blank or kit_count > 0 %}
  <section class="two-kit-comparison align-{{ section.settings.content_alignment }}">
    <div class="two-kit-comparison__outer">
      <div class="two-kit-comparison__media">
        {% if section.settings.video != blank %}
          {{
            section.settings.video
            | video_tag:
              image_size: '1920x',
              loop: true,
              autoplay: true,
              controls: false,
              muted: true,
              alt: section.settings.title
          }}
        {% elsif section.settings.image != blank %}
          {% assign imageAlt = section.settings.image.alt | default: section.settings.title %}
          {% include 'image_code' with mobileImage: section.settings.mobileImage, tabletImage: section.settings.mobileImage, desktopImage: section.settings.image, imageAlt: imageAlt, critical: true %}
        {% endif %}
      </div>

      <div class="container-1124 flex">
        <div class="two-kit-comparison__heading">
          {% if section.settings.pretitle != blank %}
            <span class="two-kit-comparison__pretitle">{{ section.settings.pretitle }}</span>
          {% endif %}

          {% if section.settings.title != blank %}
            <h2 class="{{ section.settings.text_size }} two-kit-comparison__title">{{ section.settings.title }}</h2>
          {% endif %}

          {% if section.settings.intro != blank %}
            <div class="two-kit-comparison__intro rte">
              {{ section.settings.intro }}
            </div>
          {% endif %}

          {% if section.settings.button_text != blank and section.settings.button_link != blank %}
            <a
              class="btn btn--secondary{% if section.settings.button_show_arrow %} two-kit-comparison__btn--arrow{% endif %}"
              href="{{ section.settings.button_link }}"
              title="{{ section.settings.button_text | escape }}"
              aria-label="{{ section.settings.button_text | escape }}"
              {% if section.settings.button_opens_lity %}data-lity{% endif %}
            >
              <span class="two-kit-comparison__btn-label">{{ section.settings.button_text }}</span>
              {% if section.settings.button_show_arrow %}
                <span class="two-kit-comparison__btn-icon" aria-hidden="true">
                  <img
                    src="{{ 'icon-arrow-black.svg' | asset_url }}"
                    width="14"
                    height="12"
                    alt=""
                    loading="lazy"
                  >
                </span>
              {% endif %}
            </a>
          {% endif %}
        </div>
      </div>
    </div>

    {% if kit_count > 0 %}
      <div class="container-1124 two-kit-comparison__kits">
        <div class="two-kit-comparison__grid">
          {% for block in section.blocks %}
            {% if block.type == 'kit_card' %}
              <article class="two-kit-comparison__card" {{ block.shopify_attributes }}>
                {% if block.settings.image != blank %}
                  <div class="two-kit-comparison__card-media">
                    {%- assign card_alt = block.settings.image.alt | default: block.settings.title | default: shop.name -%}
                    {{
                      block.settings.image
                      | image_url: width: 900
                      | image_tag:
                        loading: 'lazy',
                        widths: '300,450,600,750,900',
                        sizes: '(min-width: 900px) 42vw, 90vw',
                        alt: card_alt
                    }}
                  </div>
                {% endif %}

                {% if block.settings.title != blank %}
                  <h3 class="two-kit-comparison__card-title">{{ block.settings.title | escape }}</h3>
                {% endif %}

                {% if block.settings.description != blank %}
                  <div class="two-kit-comparison__card-description rte">
                    {{ block.settings.description }}
                  </div>
                {% endif %}

                {% if block.settings.bullet_list != blank %}
                  <hr class="two-kit-comparison__card-rule">
                  <div class="two-kit-comparison__card-list rte">
                    {{ block.settings.bullet_list }}
                  </div>
                {% endif %}
              </article>
            {% endif %}
          {% endfor %}
        </div>
      </div>
    {% endif %}
  </section>
{% endif %}

{% schema %}
{
  "name": "Two kit comparison",
  "tag": "section",
  "class": "section two-kit-comparison-section",
  "settings": [
    {
      "type": "header",
      "content": "Background image or video"
    },
    {
      "type": "video",
      "id": "video",
      "label": "Background video",
      "info": "If set, video is shown instead of images. Autoplays muted in a loop."
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Desktop image"
    },
    {
      "type": "image_picker",
      "id": "mobileImage",
      "label": "Mobile image"
    },
    {
      "type": "header",
      "content": "Heading"
    },
    {
      "type": "select",
      "id": "content_alignment",
      "label": "Heading alignment",
      "default": "center",
      "options": [
        { "value": "left", "label": "Left" },
        { "value": "center", "label": "Center" },
        { "value": "right", "label": "Right" }
      ]
    },
    {
      "type": "select",
      "id": "text_size",
      "label": "Title size",
      "default": "h2",
      "options": [
        { "value": "h1", "label": "H1" },
        { "value": "h2", "label": "H2" },
        { "value": "h3", "label": "H3" }
      ]
    },
    {
      "type": "text",
      "id": "pretitle",
      "label": "Pretitle",
      "default": "CHOOSE YOUR POWER SETUP"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title",
      "default": "Two ways to power your setup"
    },
    {
      "type": "richtext",
      "id": "intro",
      "label": "Intro text",
      "info": "Optional paragraph below the title."
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "BUILD MY NOMAD"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link"
    },
    {
      "type": "checkbox",
      "id": "button_show_arrow",
      "label": "Show arrow icon on button",
      "default": true
    },
    {
      "type": "checkbox",
      "id": "button_opens_lity",
      "label": "Open button link in video lightbox (Lity)",
      "default": false,
      "info": "Adds data-lity. Use a YouTube, Vimeo, or direct video file URL."
    }
  ],
  "blocks": [
    {
      "type": "kit_card",
      "name": "Comparison card",
      "limit": 2,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Kit title"
        },
        {
          "type": "richtext",
          "id": "description",
          "label": "Description"
        },
        {
          "type": "richtext",
          "id": "bullet_list",
          "label": "Bullet list",
          "info": "Use the bullet list controls in the editor."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Two kit comparison",
      "blocks": [
        {
          "type": "kit_card",
          "settings": {
            "title": "Hardwired Kit",
            "description": "<p>Best for riders who want a permanent, always-ready setup with continuous power every time they ride.</p>",
            "bullet_list": "<ul><li>Wires direct to battery on gas or electric carts</li><li>Works with batteries from 12-60V</li><li>Clean integrated install</li></ul>"
          }
        },
        {
          "type": "kit_card",
          "settings": {
            "title": "Rechargeable Kit",
            "description": "<p>Best for riders who want the simplest install possible. Charge Nomad just like a phone or tablet via USB-C</p>",
            "bullet_list": "<ul><li>No wiring needed</li><li>Fastest install</li><li>Great for flexibility</li></ul>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
